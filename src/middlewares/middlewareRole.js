// const { userAuth } = require("../middlewares/middlewareAuth");
// const roleAuth= (userAuth,async (req, res, next) => {
//     (...roles) => {
//     if (!roles.includes(req.user.role))
//       return res.status(403).json({ message: "Access Denied" });
//     next();
//   };
// });
// module.exports = {roleAuth}
